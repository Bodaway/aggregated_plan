{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Lets the HUD window subscribe to the shell's own events. Without this the generated capability set is empty (`gen/schemas/capabilities.json` was literally `{}`), so `listen()` is refused by the ACL — silently, from the webview's side — and the surface-visibility signal never reaches the page even though the Rust side emits it successfully.",
  "windows": ["hud"],
  "permissions": ["core:event:default"]
}
