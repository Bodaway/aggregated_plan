mutation AddWorklogEntry($taskId: ID!, $body: String!, $loggedAtLocal: NaiveDateTime, $sessionId: String) {
  addWorklogEntry(taskId: $taskId, body: $body, loggedAtLocal: $loggedAtLocal, sessionId: $sessionId) {
    id
    taskId
    loggedAt
    sessionId
  }
}
