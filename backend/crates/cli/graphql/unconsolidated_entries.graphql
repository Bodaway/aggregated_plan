query UnconsolidatedEntries($limit: Int!) {
  unconsolidatedWorklogEntries(limit: $limit) {
    id
    taskId
    body
    loggedAt
    task {
      id
      title
      projectId
      sourceId
    }
  }
}
