query ActivityOverlaps($date: NaiveDate!) {
  activityOverlaps(date: $date) {
    minutes
    a {
      slotId
      sessionId
      task { id title }
    }
    b {
      slotId
      sessionId
      task { id title }
    }
  }
}
