query ClaudeSession($id: String!) {
  claudeSession(id: $id) {
    id
    taskId
    task {
      id
      title
    }
    mode
    label
    startedAt
    lastSeenAt
    lastFlushAt
    endedAt
  }
}
