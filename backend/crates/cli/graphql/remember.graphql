mutation Remember($input: RememberInputGql!) {
  remember(input: $input) {
    id
    kind
    title
    body
    occurredAt
    recordedAt
    status
    source
    projectId
    taskId
    stakeholders
  }
}
